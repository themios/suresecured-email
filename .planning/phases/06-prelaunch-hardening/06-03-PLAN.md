---
plan: 06-03
phase: 06-prelaunch-hardening
wave: 2
depends_on: [06-02]
audit_ids: [PL-020, PL-021, PL-022, PL-023]
files_modified:
  - src/routes/retell.js
  - src/routes/phonecall.js
  - src/routes/webhook.js
  - src/lib/attribution.js
  - src/routes/portal.js
autonomous: true
agent: gsd-executor
skills: [api-integration-expert, backend-developer]
reviewer: gsd-integration-checker

must_haves:
  truths:
    - "Inbound Retell call sets attributed_salesperson_id on lead via voice_extension routing"
    - "Shopify order without email click credits rep when customer phone/email matches attributed lead"
    - "Orders with no resolvable attribution get commission_status pending_review — not random rep"
    - "CallRail webhook sets same first-touch attribution as Retell path"
    - "Rep sees recent call in portal linked to lead"
  artifacts:
    - path: src/lib/attribution.js
      provides: "resolveFromCallLogs(), voice source in setFirstTouchAttribution"
    - path: migrations/007_attribution.sql
      provides: "orders.commission_status column if not added in 06-02"
---

<objective>
Wire voice agent inbound calls into the same commission attribution model as email outreach.

Purpose: Voice-initiated leads must credit the correct salesperson on Shopify purchase.
Output: Retell/CallRail → first-touch attribution → order webhook resolution → portal visibility.
</objective>

<tasks>

<task id="1" audit="PL-020">
Update `retell.js` call-ended handler:

After resolving `salespersonId`:

```js
await setFirstTouchAttribution({
  leadId,
  salespersonId,
  source: 'voice_call',
  clientId,
});
```

Store `retell_call_id` on lead metadata (JSONB column or `last_voice_call_id` text field).

Gate auto-enroll on `consent_email` flag OR admin setting `clients.integration_settings.auto_enroll_voice` default false for TCPA safety.
</task>

<task id="2" audit="PL-021">
Extend `resolveSalespersonForOrder()` in attribution.js:

```
Priority after token/cart:
3. leads.attributed_salesperson_id (client-scoped)
4. call_logs: latest inbound within 90 days matching normalized phone
5. null → pending_review
```

Add to orders table:

```sql
ALTER TABLE orders ADD COLUMN IF NOT EXISTS commission_status VARCHAR(20) DEFAULT 'credited';
```

Webhook: only insert commission when status `credited`; log event with path.
</task>

<task id="3" audit="PL-023">
Unify CallRail `phonecall.js`:

- After signature verify (06-01), call `setFirstTouchAttribution` when lead matched by phone
- Optionally write to `call_logs` instead of parallel `phone_calls` — or sync both with shared helper

Deprecate duplicate logic in comments; single `recordInboundCall()` helper.
</task>

<task id="4" audit="PL-022">
Portal enhancements (minimal):

- Show last 5 `call_logs` for rep's leads
- Display `attribution_source` on lead detail if leads UI exists

Hot-lead alert (stretch): if classified reply or call transcript contains buy signals → email rep. Defer to PL-060 if timeboxed.
</task>

</tasks>

<verification>
1. Manual Appendix A Test 3 — voice call → order → commission to extension-matched rep
2. Manual: order with no attribution → pending_review, no commission row
3. gsd-integration-checker: voice + email paths both land in same attribution table
</verification>
