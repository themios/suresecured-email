---
plan: 06-01
phase: 06-prelaunch-hardening
wave: 1
depends_on: []
audit_ids: [PL-001, PL-002, PL-003, PL-006, PL-007, PL-008]
files_modified:
  - src/routes/api.js
  - src/routes/analytics.js
  - src/routes/phonecall.js
  - src/routes/retell.js
  - src/routes/telnyx.js
  - src/routes/auth.js
  - src/routes/admin.js
  - src/routes/cron.js
  - src/setup.js
  - src/index.js
  - .env.example
  - railway.toml
autonomous: true
agent: gsd-executor
skills: [security-scan, backend-developer]
reviewer: security-engineer

must_haves:
  truths:
    - "Unauthenticated requests to /api/stats and /api/leads return 401"
    - "Shopify webhook remains the only unauthenticated write path that accepts public traffic — and it verifies HMAC"
    - "CallRail, Retell, and Telnyx webhooks reject requests without valid signatures"
    - "Analytics ?days= and ?sp= parameters cannot inject SQL"
    - "POST /cron/send-sequences succeeds with Railway cron Authorization header"
    - "setup.js creates users-table owner; change-password updates users table"
    - "Admin mutation routes require operator/owner/admin role"
  artifacts:
    - path: src/middleware/apiAuth.js
      provides: "Optional: shared API key + requireAuth helper for /api"
    - path: .env.example
      provides: "CALLRAIL_WEBHOOK_SECRET, RETELL_WEBHOOK_SECRET, TELNYX_PUBLIC_KEY documented"
---

<objective>
Close P0 security launch blockers before any attribution or deliverability work.

Purpose: Prevent data breach, webhook forgery, and SQL injection before production traffic.
Output: Hardened API, webhooks, auth consistency, cron method fix, baseline rate limits + helmet.
</objective>

<tasks>

<task id="1" audit="PL-007">
Fix cron HTTP mismatch in `src/routes/cron.js`:

```js
const cronRoutes = ['send-sequences', 'daily-digest', 'score-leads'];
cronRoutes.forEach(path => {
  router.all(`/${path}`, cronAuth, handlers[path]);
});
```

Verify: `curl -X POST $TRACKER_URL/cron/send-sequences -H "Authorization: Bearer $CRON_SECRET"` returns 200 JSON.
</task>

<task id="2" audit="PL-006">
Auth consistency:

1. `src/setup.js` — insert into `users` (role `owner`) not `admin_users`; link to SureSecured org/client from migration seed.
2. `src/routes/admin.js` change-password — query/update `users` table.
3. Add `requireRole('operator','owner','admin')` on all admin POST routes.
4. Gate `SEED_OPERATOR=1` behind `NODE_ENV !== 'production'`.
</task>

<task id="3" audit="PL-001">
Lock `/api`:

- Add `requireAuth` + `requireRole` on admin endpoints (stats, salespeople, leads, landing-page mutations, suppression).
- Create `POST /api/form-submission` auth via `X-Client-Api-Key` header validated against `clients.integration_settings->>'api_key_hash'`.
- Document key generation in admin client edit UI (optional minimal: env `CLIENT_API_KEY` for v1).

Remove or redirect duplicate dangerous routes if redundant with `/sequences/api/*`.
</task>

<task id="4" audit="PL-002">
Webhook verification:

| Route | Implementation |
|-------|------------------|
| `phonecall.js` | HMAC or shared secret header per CallRail docs |
| `retell.js` | Verify Retell signature header |
| `telnyx.js` | Verify `telnyx-signature-ed25519` |

Return 401 on failure. Log verification failures without logging full body.
</task>

<task id="5" audit="PL-003">
Parameterize SQL in:

- `src/routes/analytics.js` — replace `${days}` and `${spFilter}` with `$1`, `$2`.
- `src/routes/api.js` landing-page GET — build parameterized WHERE clause.

Add grep CI check or comment: no template literals inside pool.query SQL strings with user input.
</task>

<task id="6" audit="PL-008">
Platform hardening in `src/index.js`:

- `helmet()` with sensible defaults
- `express-rate-limit` on `/login`, `/portal/login`, `/api`, `/cron`
- Separate `UNSUBSCRIBE_HMAC_SECRET` in unsubscribe.js (fallback to JWT_SECRET with deprecation log)

Update `.env.example` with new secrets.
</task>

</tasks>

<verification>
1. Manual: curl /api/stats → 401
2. Manual: forged webhook → 401
3. Manual: analytics injection attempt → safe
4. Manual: POST cron → 200
5. Run: `node src/lib/commissions.test.js` (regression)
6. Agent: security-engineer reviews diff before merge
</verification>
