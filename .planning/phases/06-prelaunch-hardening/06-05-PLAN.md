---
plan: 06-05
phase: 06-prelaunch-hardening
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04]
audit_ids: [PL-050, PL-051, Appendix-A, Go-No-Go]
files_modified:
  - src/lib/attribution.test.js
  - src/lib/unsubscribe.test.js
  - src/routes/webhook.test.js
  - package.json
  - .github/workflows/test.yml
autonomous: partial
agent: gsd-verifier
skills: [qa-engineer, deploy-check]
subagents: [gsd-integration-checker, testing-reality-checker]

must_haves:
  truths:
    - "Appendix A Tests 1–5 pass in staging environment"
    - "npm test runs unit tests in CI"
    - "PRELAUNCH_AUDIT.md go/no-go checklist fully checked"
    - "06-VERIFICATION.md written with goal-backward pass/fail"
  artifacts:
    - path: .planning/phases/06-prelaunch-hardening/06-VERIFICATION.md
      provides: "Phase 6 sign-off report"
    - path: .planning/phases/06-prelaunch-hardening/06-SUMMARY.md
      provides: "Execution summary after all plans complete"
---

<objective>
Prove Phase 6 goal achievement through automated tests, integration verification, and formal go/no-go sign-off.

Purpose: No production enrollment until evidence exists.
Output: Test suite, verification doc, monitoring hooks, launch approval record.
</objective>

<tasks>

<task id="1" audit="PL-050">
Add test script to package.json:

```json
"test": "node src/lib/commissions.test.js && node src/lib/attribution.test.js && node src/lib/unsubscribe.test.js"
```

Create minimal assert-based tests (no new framework — matches project style):

- `attribution.test.js` — first-touch, resolution order, pending_review
- `unsubscribe.test.js` — token round-trip
- `webhook.test.js` — Shopify HMAC verify function exported for test

Optional: GitHub Actions workflow on push.
</task>

<task id="2" audit="Appendix-A">
Execute E2E manual script from PRELAUNCH_AUDIT.md Appendix A in staging:

| Test | Owner |
|------|-------|
| Email click → purchase → commission | gsd-integration-checker |
| Reply pauses sequence | gsd-integration-checker |
| Voice → purchase → commission | gsd-integration-checker |
| Unsubscribe | gsd-integration-checker |
| Security smoke | security-engineer |

Record results in `06-VERIFICATION.md` with screenshots/log snippets.
</task>

<task id="3" audit="PL-051">
Monitoring minimum:

- Structured log prefix `[cron]`, `[webhook]`, `[attribution]`
- Alert if cron returns errors > 0 two consecutive runs
- Daily ops email already exists via digest — add bounce rate line

Document Railway log drain or Sentry DSN in runbook.
</task>

<task id="4">
gsd-verifier goal-backward analysis:

Read must_haves from 06-01 through 06-04. For each truth, cite file:line evidence or mark FAIL with remediation task.

Produce `06-VERIFICATION.md` using same format as `03-VERIFICATION.md`.
</task>

<task id="5">
Go/no-go meeting checklist from PRELAUNCH_AUDIT.md Appendix C — all boxes must be checked with sign-off names/dates in 06-VERIFICATION.md.
</task>

</tasks>

<verification>
testing-reality-checker agent reviews evidence — default NEEDS WORK until Appendix A passes.
Only after PASS: update ROADMAP Phase 6 to Complete, STATE.md current focus = PRELAUNCH READY.
</verification>
