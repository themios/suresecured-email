---
plan: 06-04
phase: 06-prelaunch-hardening
wave: 3
depends_on: [06-01]
audit_ids: [PL-030, PL-031, PL-032, PL-033, PL-034, PL-035, PL-040]
files_modified:
  - migrations/008_send_limits.sql
  - src/routes/cron.js
  - src/lib/gmail.js
  - src/routes/sequences.js
  - src/routes/admin.js
autonomous: partial
agent: gsd-executor
skills: [backend-developer, deploy-check, cold-email]
human_ops: [PL-030, PL-034, PL-035]

> **Operator override (2026-07-08):** SureSecured launch uses **offline bulk verify** + CSV `preverified` import instead of in-app ZeroBounce. See `DECISIONS.md` B3, DELV-01b.

must_haves:
  truths:
    - "Cron skips send when lead.email_verified IS NOT TRUE"
    - "Each email_account respects daily_send_limit with warmup ramp by week"
    - "Bounce rate > 3% in 24h pauses enrollments for that inbox"
    - "List-Unsubscribe header present on outbound HTML emails"
    - "Admin UI shows sends today / limit per Gmail connection"
  artifacts:
    - path: migrations/008_send_limits.sql
      provides: "email_accounts daily counters + warmup columns"
    - path: docs/DELIVERABILITY_RUNBOOK.md
      provides: "DNS checklist, staged rollout, Instantly decision"
---

<objective>
Prevent domain blacklisting by enforcing verification gates, send caps, and bounce circuit breakers before mass outreach.

Purpose: SureSecured root domain and rep subdomains must stay clean for 40k reactivation.
Output: Send limit engine, verification gate in cron, circuit breaker, deliverability runbook for ops.
</objective>

<tasks>

<task id="1" audit="PL-031">
In `cron.js` send-sequences loop, before send:

```js
if (row.email_verified !== true) {
  // skip + optional log metric unverified_skipped
  continue;
}
```

Add sequences UI banner: count of unverified leads blocking send.

Optional: new cron `POST /cron/verify-batch` that calls ZeroBounce for 50/hour automatically.
</task>

<task id="2" audit="PL-032">
Migration `008_send_limits.sql`:

```sql
ALTER TABLE email_accounts ADD COLUMN IF NOT EXISTS
  daily_send_limit INTEGER DEFAULT 20,
  sends_today INTEGER DEFAULT 0,
  sends_today_date DATE,
  warmup_start_date DATE DEFAULT CURRENT_DATE;
```

Helper `getEffectiveDailyLimit(account)` — week 1: 5, week 2: 10, week 3: 20, week 4: 40, week 5+: min(configured, 80).

Before send: increment counter; skip if at limit. Reset at date rollover (America/Los_Angeles).

Admin UI: show limit usage on sequences Gmail table.
</task>

<task id="3" audit="PL-033">
Circuit breaker:

- After each send batch, compute bounce rate per salesperson inbox from `email_sends` last 24h
- If > 3%: `UPDATE email_accounts SET enabled = false, last_error = 'circuit_breaker_bounce'`
- Pause active enrollments for that rep's sends

Stretch: Gmail API poll for DSN bounces — document as P2 if not in scope.
</task>

<task id="4" audit="PL-034">
Code: add List-Unsubscribe headers in `buildRawMessage` / nodemailer options:

```
List-Unsubscribe: <url>, <mailto:...>
List-Unsubscribe-Post: List-Unsubscribe=One-Click
```

Human ops doc `docs/DELIVERABILITY_RUNBOOK.md`:

- SPF/DKIM/DMARC per subdomain checklist
- Google Postmaster registration
- mail-tester.com validation step
- **Decision record:** Instantly.ai vs built-in sender (PL-030)
</task>

<task id="5" audit="PL-035">
Runbook section — staged rollout:

| Stage | Volume | Gate |
|-------|--------|------|
| Pilot | 500 | bounce < 2%, open > 20% |
| Scale 1 | 2,000 | 48h review |
| Scale 2 | 10,000 | 48h review |
| Full | remainder | weekly review |

Require Shopify customer suppression upload before stage 1.
</task>

<task id="6" audit="PL-040">
Add consent columns to leads (migration):

```sql
consent_email BOOLEAN DEFAULT false,
consent_sms BOOLEAN DEFAULT false,
```

CSV import: map `consent_email` column. Historical list: document re-permission strategy in runbook.

SMS cron branch: skip if `!consent_sms`.
</task>

</tasks>

<verification>
1. Seed unverified lead in enrollment → cron skips (sent count unchanged)
2. Set sends_today at limit → next send skipped
3. Simulate bounce rate > 3% → inbox disabled
4. Inspect raw email headers for List-Unsubscribe
5. Ops sign-off on DNS checklist (human)
</verification>
