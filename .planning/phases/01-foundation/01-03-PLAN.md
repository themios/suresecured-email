---
plan: 01-03
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/routes/admin.js
  - src/views/admin/clients.ejs
  - src/views/admin/client-edit.ejs
autonomous: true

must_haves:
  truths:
    - "Operator can navigate to /admin/clients and see a list of all client records"
    - "Operator can click 'New Client' and fill in name, slug, brand config, commission rules, and integration settings"
    - "Saving the form upserts to the clients table with the JSONB fields populated"
    - "Operator can edit an existing client record and the form pre-populates with saved values"
  artifacts:
    - path: "src/routes/admin.js"
      provides: "GET /admin/clients, POST /admin/clients, GET /admin/clients/:id/edit, POST /admin/clients/:id"
    - path: "src/views/admin/clients.ejs"
      provides: "Client list page"
    - path: "src/views/admin/client-edit.ejs"
      provides: "Create/edit form for a single client with brand_config, commission_rules, integration_settings sections"
  key_links:
    - from: "client-edit.ejs form POST"
      to: "POST /admin/clients or POST /admin/clients/:id"
      via: "HTML form action with method POST"
    - from: "POST handler"
      to: "clients table"
      via: "INSERT ... ON CONFLICT (id) DO UPDATE with JSONB fields"
---

<objective>
Add create/edit client management UI to the admin panel. Operator can create a new client under an organization and configure brand, commission, and integration settings stored in JSONB columns.

Purpose: This is the operator control plane — without it no client can be configured, branding stays hardcoded, and commission rules cannot be set per client.
Output: Four new routes in admin.js and two EJS views for client list and client edit form.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/routes/admin.js
@src/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add client CRUD routes to admin.js</name>
  <files>src/routes/admin.js</files>
  <action>
Add the following four routes to `src/routes/admin.js`. Insert them after existing route definitions, before `module.exports`. Use the existing `pool` import (or add `const { pool } = require('../db');` if not already imported).

All four routes must be guarded by `requireAuth` (already applied to the router or to each route — match the existing pattern in the file).

```js
// GET /admin/clients — list all clients with org name
router.get('/clients', requireAuth, async (req, res) => {
  const { rows } = await pool.query(`
    SELECT c.id, c.name, c.slug, c.active, o.name AS org_name
    FROM clients c
    JOIN organizations o ON o.id = c.organization_id
    ORDER BY o.name, c.name
  `);
  res.render('admin/clients', { clients: rows });
});

// GET /admin/clients/new — blank create form
router.get('/clients/new', requireAuth, async (req, res) => {
  const { rows: orgs } = await pool.query('SELECT id, name FROM organizations ORDER BY name');
  res.render('admin/client-edit', { client: null, orgs, errors: [] });
});

// POST /admin/clients — create new client
router.post('/clients', requireAuth, async (req, res) => {
  const { organization_id, name, slug, brand_config, commission_rules, integration_settings } = req.body;
  const errors = [];
  if (!name || name.trim().length < 2) errors.push('Name must be at least 2 characters');
  if (!slug || !/^[a-z0-9-]+$/.test(slug)) errors.push('Slug must be lowercase alphanumeric with hyphens only');
  if (errors.length) {
    const { rows: orgs } = await pool.query('SELECT id, name FROM organizations ORDER BY name');
    return res.status(400).render('admin/client-edit', { client: null, orgs, errors });
  }
  // Parse JSONB fields — form sends them as JSON strings
  const brandJson = parseJsonField(brand_config, {});
  const commJson  = parseJsonField(commission_rules, {});
  const intJson   = parseJsonField(integration_settings, {});
  await pool.query(
    `INSERT INTO clients (organization_id, name, slug, brand_config, commission_rules, integration_settings)
     VALUES ($1, $2, $3, $4, $5, $6)`,
    [organization_id, name.trim(), slug.trim(), JSON.stringify(brandJson), JSON.stringify(commJson), JSON.stringify(intJson)]
  );
  res.redirect('/admin/clients');
});

// GET /admin/clients/:id/edit — pre-populated edit form
router.get('/clients/:id/edit', requireAuth, async (req, res) => {
  const { rows } = await pool.query('SELECT * FROM clients WHERE id = $1', [req.params.id]);
  if (!rows.length) return res.status(404).send('Client not found');
  const { rows: orgs } = await pool.query('SELECT id, name FROM organizations ORDER BY name');
  res.render('admin/client-edit', { client: rows[0], orgs, errors: [] });
});

// POST /admin/clients/:id — update existing client
router.post('/clients/:id', requireAuth, async (req, res) => {
  const { name, slug, brand_config, commission_rules, integration_settings, active } = req.body;
  const brandJson = parseJsonField(brand_config, {});
  const commJson  = parseJsonField(commission_rules, {});
  const intJson   = parseJsonField(integration_settings, {});
  await pool.query(
    `UPDATE clients
     SET name=$1, slug=$2, brand_config=$3, commission_rules=$4,
         integration_settings=$5, active=$6
     WHERE id=$7`,
    [name.trim(), slug.trim(), JSON.stringify(brandJson), JSON.stringify(commJson),
     JSON.stringify(intJson), active === 'on', req.params.id]
  );
  res.redirect('/admin/clients');
});
```

Add this helper function near the top of the file (after requires):
```js
function parseJsonField(value, fallback) {
  if (!value) return fallback;
  if (typeof value === 'object') return value;
  try { return JSON.parse(value); } catch { return fallback; }
}
```

If the existing router uses `router.use(requireAuth)` at the top level, do NOT add requireAuth to individual routes — match the pattern already in the file.
  </action>
  <verify>
```bash
node -e "
const admin = require('./src/routes/admin');
console.log('Routes loaded OK');
process.exit(0);
"
# Should print "Routes loaded OK" with no errors
```
  </verify>
  <done>src/routes/admin.js loads without errors. All 4 new routes are registered (GET /clients, GET /clients/new, POST /clients, GET /clients/:id/edit, POST /clients/:id).</done>
</task>

<task type="auto">
  <name>Task 2: Create EJS views for client list and client edit form</name>
  <files>src/views/admin/clients.ejs, src/views/admin/client-edit.ejs</files>
  <action>
Determine the views directory by checking where existing admin views live: `ls src/views/` or `ls views/`. Use the same directory.

**src/views/admin/clients.ejs** (or views/admin/clients.ejs — match existing path):
```html
<%- include('../partials/header', { title: 'Clients' }) %>
<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Clients</h2>
    <a href="/admin/clients/new" class="btn btn-primary">+ New Client</a>
  </div>
  <% if (!clients.length) { %>
    <p class="text-muted">No clients yet. Create one above.</p>
  <% } else { %>
  <table class="table table-striped">
    <thead><tr><th>Name</th><th>Org</th><th>Slug</th><th>Active</th><th></th></tr></thead>
    <tbody>
      <% clients.forEach(c => { %>
      <tr>
        <td><%= c.name %></td>
        <td><%= c.org_name %></td>
        <td><code><%= c.slug %></code></td>
        <td><%= c.active ? 'Yes' : 'No' %></td>
        <td><a href="/admin/clients/<%= c.id %>/edit" class="btn btn-sm btn-secondary">Edit</a></td>
      </tr>
      <% }) %>
    </tbody>
  </table>
  <% } %>
</div>
<%- include('../partials/footer') %>
```

If the existing views use a different include pattern (e.g., layout extends, or no partials directory), match that pattern instead of the include lines above. The table structure and form logic below are required regardless.

**src/views/admin/client-edit.ejs**:
```html
<%- include('../partials/header', { title: client ? 'Edit Client' : 'New Client' }) %>
<div class="container mt-4" style="max-width:720px">
  <h2><%= client ? 'Edit Client: ' + client.name : 'New Client' %></h2>

  <% if (errors.length) { %>
    <div class="alert alert-danger">
      <ul class="mb-0"><% errors.forEach(e => { %><li><%= e %></li><% }) %></ul>
    </div>
  <% } %>

  <form method="POST" action="<%= client ? '/admin/clients/' + client.id : '/admin/clients' %>">
    <div class="mb-3">
      <label class="form-label">Organization</label>
      <select name="organization_id" class="form-select" required>
        <% orgs.forEach(o => { %>
          <option value="<%= o.id %>" <%= (client && client.organization_id == o.id) ? 'selected' : '' %>>
            <%= o.name %>
          </option>
        <% }) %>
      </select>
    </div>
    <div class="mb-3">
      <label class="form-label">Client Name</label>
      <input type="text" name="name" class="form-control" required
             value="<%= client ? client.name : '' %>">
    </div>
    <div class="mb-3">
      <label class="form-label">Slug (lowercase, hyphens only)</label>
      <input type="text" name="slug" class="form-control" required pattern="[a-z0-9-]+"
             value="<%= client ? client.slug : '' %>">
    </div>

    <h5 class="mt-4">Brand Config (JSON)</h5>
    <p class="text-muted small">Keys: primary_color, accent_color, bg_color, name, phone, website, address, cta_url, cta_label</p>
    <div class="mb-3">
      <textarea name="brand_config" class="form-control font-monospace" rows="8"><%= client ? JSON.stringify(client.brand_config, null, 2) : '{}' %></textarea>
    </div>

    <h5 class="mt-4">Commission Rules (JSON)</h5>
    <div class="mb-3">
      <textarea name="commission_rules" class="form-control font-monospace" rows="4"><%= client ? JSON.stringify(client.commission_rules, null, 2) : '{}' %></textarea>
    </div>

    <h5 class="mt-4">Integration Settings (JSON)</h5>
    <div class="mb-3">
      <textarea name="integration_settings" class="form-control font-monospace" rows="4"><%= client ? JSON.stringify(client.integration_settings, null, 2) : '{}' %></textarea>
    </div>

    <% if (client) { %>
    <div class="mb-3 form-check">
      <input type="checkbox" class="form-check-input" name="active" id="active"
             <%= client.active ? 'checked' : '' %>>
      <label class="form-check-label" for="active">Active</label>
    </div>
    <% } %>

    <button type="submit" class="btn btn-success">Save Client</button>
    <a href="/admin/clients" class="btn btn-link">Cancel</a>
  </form>
</div>
<%- include('../partials/footer') %>
```

If the project uses a different template engine or layout mechanism, adapt the wrapper (include/extend/block tags) to match. The form field names and POST action URLs must remain exactly as specified.
  </action>
  <verify>
```bash
# Start server and check routes render (requires DB to be running)
node src/index.js &
sleep 2
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/admin/clients
# Will redirect to /login (302) if not authenticated — that's correct
kill %1
```
  </verify>
  <done>Both EJS files exist on disk. GET /admin/clients returns 302 redirect to login when unauthenticated (confirming route is registered and protected). Form fields name, slug, brand_config, commission_rules, integration_settings are present in client-edit.ejs.</done>
</task>

</tasks>

<verification>
```bash
# 1. View files exist
ls src/views/admin/clients.ejs src/views/admin/client-edit.ejs

# 2. Routes are registered (check route stack)
node -e "
const app = require('./src/index');
const routes = app._router.stack
  .filter(r => r.route)
  .map(r => Object.keys(r.route.methods)[0].toUpperCase() + ' ' + r.route.path);
console.log(routes.join('\n'));
" 2>/dev/null | grep -i client
# Expected: lines containing GET /admin/clients and POST /admin/clients

# 3. Create client via curl (with auth cookie from 01-02 plan tests)
# Login first, then POST new client
curl -s -c /tmp/ct.txt -X POST http://localhost:3000/auth/login \
  -H 'Content-Type: application/json' -d '{"email":"operator@suresecured.com","password":"operator123"}'
curl -s -b /tmp/ct.txt -X POST http://localhost:3000/admin/clients \
  -d 'organization_id=1&name=TestClient&slug=test-client&brand_config={}&commission_rules={}&integration_settings={}'
psql $DATABASE_URL -c "SELECT id, name, slug FROM clients WHERE slug='test-client'"
# => 1 row
```
</verification>

<success_criteria>
- GET /admin/clients renders client list table
- GET /admin/clients/new renders empty form
- POST /admin/clients inserts row with JSONB fields into clients table
- GET /admin/clients/:id/edit pre-populates form with saved values
- POST /admin/clients/:id updates the client row
- Invalid name/slug returns 400 with error messages in view
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` with what was built, key decisions, and any deviations from this plan.
</output>
